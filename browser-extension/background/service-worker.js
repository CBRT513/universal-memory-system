/**
 * Universal AI Memory Capture - Background Service Worker
 * Handles extension lifecycle, messaging, and background operations
 */

class MemoryServiceWorker {
    constructor() {
        this.apiBase = 'http://localhost:8091';
        this.connections = new Map();
        this.settings = {};
        
        this.init();
    }
    
    /**
     * Initialize service worker
     */
    init() {
        console.log('🧠 Memory service worker initializing...');
        
        // Set up event listeners
        this.setupEventListeners();
        
        // Load settings
        this.loadSettings();
        
        console.log('✅ Memory service worker initialized');
    }
    
    /**
     * Set up event listeners
     */
    setupEventListeners() {
        // Extension installation/update
        chrome.runtime.onInstalled.addListener((details) => {
            this.handleInstalled(details);
        });
        
        // Extension startup
        chrome.runtime.onStartup.addListener(() => {
            this.handleStartup();
        });
        
        // Messages from content scripts and popup
        chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
            this.handleMessage(message, sender, sendResponse);
            return true; // Keep message channel open for async response
        });
        
        // Tab updates (for monitoring navigation)
        chrome.tabs.onUpdated.addListener((tabId, changeInfo, tab) => {
            this.handleTabUpdated(tabId, changeInfo, tab);
        });
        
        // Commands (keyboard shortcuts)
        chrome.commands.onCommand.addListener((command) => {
            this.handleCommand(command);
        });
        
        // Storage changes
        chrome.storage.onChanged.addListener((changes, areaName) => {
            this.handleStorageChanged(changes, areaName);
        });
        
        // Alarms for periodic tasks
        chrome.alarms.onAlarm.addListener((alarm) => {
            this.handleAlarm(alarm);
        });
        
        // Context menus
        this.setupContextMenus();
    }
    
    /**\n     * Handle extension installation/update\n     */\n    async handleInstalled(details) {\n        console.log('📦 Extension installed/updated:', details.reason);\n        \n        if (details.reason === 'install') {\n            // First time installation\n            await this.setupDefaultSettings();\n            await this.showWelcomeNotification();\n        } else if (details.reason === 'update') {\n            // Extension updated\n            console.log(`📈 Updated from ${details.previousVersion} to ${chrome.runtime.getManifest().version}`);\n        }\n        \n        // Set up periodic health checks\n        chrome.alarms.create('health-check', { periodInMinutes: 5 });\n    }\n    \n    /**\n     * Handle extension startup\n     */\n    handleStartup() {\n        console.log('🚀 Extension started');\n        this.loadSettings();\n    }\n    \n    /**\n     * Handle messages from content scripts and popup\n     */\n    async handleMessage(message, sender, sendResponse) {\n        console.log('📨 Received message:', message.action, 'from:', sender.tab?.url || 'popup');\n        \n        try {\n            switch (message.action) {\n                case 'capture-content':\n                    await this.handleCaptureContent(message, sender, sendResponse);\n                    break;\n                    \n                case 'quick-capture':\n                    await this.handleQuickCapture(message, sender, sendResponse);\n                    break;\n                    \n                case 'get-platform':\n                    sendResponse({ platform: this.detectPlatform(sender.tab?.url) });\n                    break;\n                    \n                case 'check-connection':\n                    await this.checkMemoryService(sendResponse);\n                    break;\n                    \n                case 'get-settings':\n                    sendResponse({ settings: this.settings });\n                    break;\n                    \n                case 'update-setting':\n                    await this.updateSetting(message.key, message.value);\n                    sendResponse({ success: true });\n                    break;\n                    \n                case 'get-stats':\n                    await this.getStats(sendResponse);\n                    break;\n                    \n                default:\n                    console.warn('Unknown message action:', message.action);\n                    sendResponse({ error: 'Unknown action' });\n            }\n        } catch (error) {\n            console.error('Message handling error:', error);\n            sendResponse({ error: error.message });\n        }\n    }\n    \n    /**\n     * Handle tab updates\n     */\n    handleTabUpdated(tabId, changeInfo, tab) {\n        if (changeInfo.status === 'complete' && tab.url) {\n            const platform = this.detectPlatform(tab.url);\n            \n            if (platform !== 'unknown') {\n                console.log(`🎯 AI platform detected: ${platform} on tab ${tabId}`);\n                \n                // Inject content scripts if needed\n                this.ensureContentScriptInjected(tabId, tab.url);\n            }\n        }\n    }\n    \n    /**\n     * Handle keyboard commands\n     */\n    async handleCommand(command) {\n        console.log('⌨️ Command executed:', command);\n        \n        switch (command) {\n            case 'quick-capture':\n                await this.executeQuickCapture();\n                break;\n                \n            case 'toggle-capture':\n                await this.toggleCaptureMode();\n                break;\n                \n            default:\n                console.warn('Unknown command:', command);\n        }\n    }\n    \n    /**\n     * Handle storage changes\n     */\n    handleStorageChanged(changes, areaName) {\n        if (areaName === 'sync') {\n            console.log('⚙️ Settings changed:', changes);\n            this.loadSettings();\n        }\n    }\n    \n    /**\n     * Handle alarms\n     */\n    async handleAlarm(alarm) {\n        console.log('⏰ Alarm triggered:', alarm.name);\n        \n        switch (alarm.name) {\n            case 'health-check':\n                await this.performHealthCheck();\n                break;\n        }\n    }\n    \n    /**\n     * Set up context menus\n     */\n    setupContextMenus() {\n        // Clear existing menus\n        chrome.contextMenus.removeAll(() => {\n            // Add memory capture context menu\n            chrome.contextMenus.create({\n                id: 'capture-selection',\n                title: '🧠 Capture to Memory',\n                contexts: ['selection'],\n                documentUrlPatterns: [\n                    'https://chat.openai.com/*',\n                    'https://claude.ai/*',\n                    'https://www.perplexity.ai/*',\n                    'https://bard.google.com/*',\n                    'https://www.bing.com/*',\n                    'https://huggingface.co/*'\n                ]\n            });\n            \n            // Add page capture context menu\n            chrome.contextMenus.create({\n                id: 'capture-page',\n                title: '📄 Capture Page Info',\n                contexts: ['page'],\n                documentUrlPatterns: [\n                    'https://chat.openai.com/*',\n                    'https://claude.ai/*',\n                    'https://www.perplexity.ai/*',\n                    'https://bard.google.com/*',\n                    'https://www.bing.com/*',\n                    'https://huggingface.co/*'\n                ]\n            });\n            \n            console.log('📋 Context menus created');\n        });\n        \n        // Handle context menu clicks\n        chrome.contextMenus.onClicked.addListener(async (info, tab) => {\n            await this.handleContextMenuClick(info, tab);\n        });\n    }\n    \n    /**\n     * Handle context menu clicks\n     */\n    async handleContextMenuClick(info, tab) {\n        console.log('🖱️ Context menu clicked:', info.menuItemId);\n        \n        try {\n            switch (info.menuItemId) {\n                case 'capture-selection':\n                    if (info.selectionText) {\n                        await this.captureSelection(info.selectionText, tab);\n                    }\n                    break;\n                    \n                case 'capture-page':\n                    await this.capturePage(tab);\n                    break;\n            }\n        } catch (error) {\n            console.error('Context menu error:', error);\n            this.showNotification('❌ Capture failed', error.message);\n        }\n    }\n    \n    /**\n     * Detect AI platform from URL\n     */\n    detectPlatform(url) {\n        if (!url) return 'unknown';\n        \n        try {\n            const hostname = new URL(url).hostname;\n            \n            if (hostname.includes('openai.com')) return 'chatgpt';\n            if (hostname.includes('claude.ai')) return 'claude';\n            if (hostname.includes('perplexity.ai')) return 'perplexity';\n            if (hostname.includes('bard.google.com')) return 'bard';\n            if (hostname.includes('bing.com') && url.includes('chat')) return 'bing';\n            if (hostname.includes('huggingface.co')) return 'huggingface';\n            \n            return 'unknown';\n        } catch (error) {\n            return 'unknown';\n        }\n    }\n    \n    /**\n     * Ensure content script is injected\n     */\n    async ensureContentScriptInjected(tabId, url) {\n        try {\n            // Check if content script is already injected\n            const results = await chrome.scripting.executeScript({\n                target: { tabId },\n                function: () => {\n                    return typeof window.aiMemoryMonitor !== 'undefined';\n                }\n            });\n            \n            if (!results[0]?.result) {\n                console.log('💉 Injecting content scripts into tab', tabId);\n                \n                // Inject CSS first\n                await chrome.scripting.insertCSS({\n                    target: { tabId },\n                    files: ['content-scripts/capture-ui.css']\n                });\n                \n                // Inject scripts\n                await chrome.scripting.executeScript({\n                    target: { tabId },\n                    files: [\n                        'content-scripts/ai-monitor.js',\n                        'content-scripts/capture-interface.js'\n                    ]\n                });\n                \n                console.log('✅ Content scripts injected successfully');\n            }\n        } catch (error) {\n            console.error('Content script injection failed:', error);\n        }\n    }\n    \n    /**\n     * Load settings from storage\n     */\n    async loadSettings() {\n        try {\n            const result = await chrome.storage.sync.get({\n                autoDetect: true,\n                showNotifications: true,\n                smartTagging: true,\n                apiBase: 'http://localhost:8091'\n            });\n            \n            this.settings = result;\n            this.apiBase = result.apiBase;\n            \n            console.log('⚙️ Settings loaded:', this.settings);\n        } catch (error) {\n            console.error('Error loading settings:', error);\n        }\n    }\n    \n    /**\n     * Set up default settings on first install\n     */\n    async setupDefaultSettings() {\n        const defaultSettings = {\n            autoDetect: true,\n            showNotifications: true,\n            smartTagging: true,\n            apiBase: 'http://localhost:8091',\n            version: chrome.runtime.getManifest().version\n        };\n        \n        await chrome.storage.sync.set(defaultSettings);\n        console.log('🔧 Default settings applied');\n    }\n    \n    /**\n     * Update a setting\n     */\n    async updateSetting(key, value) {\n        this.settings[key] = value;\n        await chrome.storage.sync.set({ [key]: value });\n        console.log(`Setting updated: ${key} = ${value}`);\n    }\n    \n    /**\n     * Check memory service health\n     */\n    async checkMemoryService(sendResponse) {\n        try {\n            const response = await fetch(`${this.apiBase}/api/health`);\n            const health = await response.json();\n            \n            sendResponse({ \n                connected: true, \n                health,\n                apiBase: this.apiBase\n            });\n            \n        } catch (error) {\n            console.error('Memory service check failed:', error);\n            sendResponse({ \n                connected: false, \n                error: error.message,\n                apiBase: this.apiBase\n            });\n        }\n    }\n    \n    /**\n     * Get system statistics\n     */\n    async getStats(sendResponse) {\n        try {\n            const response = await fetch(`${this.apiBase}/api/memory/stats`);\n            if (response.ok) {\n                const data = await response.json();\n                sendResponse({ success: true, stats: data.stats });\n            } else {\n                throw new Error(`HTTP ${response.status}`);\n            }\n        } catch (error) {\n            console.error('Error getting stats:', error);\n            sendResponse({ success: false, error: error.message });\n        }\n    }\n    \n    /**\n     * Capture selected text\n     */\n    async captureSelection(text, tab) {\n        try {\n            const platform = this.detectPlatform(tab.url);\n            \n            const response = await fetch(`${this.apiBase}/api/memory/capture-ai`, {\n                method: 'POST',\n                headers: {\n                    'Content-Type': 'application/json'\n                },\n                body: JSON.stringify({\n                    content: text,\n                    ai_context: {\n                        platform,\n                        url: tab.url,\n                        title: tab.title,\n                        timestamp: new Date().toISOString(),\n                        capture_method: 'context_menu'\n                    }\n                })\n            });\n            \n            if (!response.ok) {\n                throw new Error(`HTTP ${response.status}`);\n            }\n            \n            const result = await response.json();\n            \n            if (this.settings.showNotifications) {\n                this.showNotification('✅ Memory Captured', `Saved as ${result.category} with ${result.tags.length} tags`);\n            }\n            \n        } catch (error) {\n            console.error('Selection capture failed:', error);\n            this.showNotification('❌ Capture Failed', error.message);\n        }\n    }\n    \n    /**\n     * Capture page information\n     */\n    async capturePage(tab) {\n        try {\n            const platform = this.detectPlatform(tab.url);\n            const content = `Page: ${tab.title}\\nURL: ${tab.url}\\nPlatform: ${platform}`;\n            \n            const response = await fetch(`${this.apiBase}/api/memory/store`, {\n                method: 'POST',\n                headers: {\n                    'Content-Type': 'application/json'\n                },\n                body: JSON.stringify({\n                    content,\n                    category: 'reference',\n                    tags: [platform, 'page-capture'],\n                    source: 'browser_extension_context',\n                    source_url: tab.url,\n                    metadata: {\n                        title: tab.title,\n                        platform,\n                        captured_at: new Date().toISOString()\n                    }\n                })\n            });\n            \n            if (!response.ok) {\n                throw new Error(`HTTP ${response.status}`);\n            }\n            \n            if (this.settings.showNotifications) {\n                this.showNotification('✅ Page Captured', `${tab.title} saved to memory`);\n            }\n            \n        } catch (error) {\n            console.error('Page capture failed:', error);\n            this.showNotification('❌ Capture Failed', error.message);\n        }\n    }\n    \n    /**\n     * Execute quick capture command\n     */\n    async executeQuickCapture() {\n        try {\n            const [tab] = await chrome.tabs.query({ active: true, currentWindow: true });\n            \n            if (!tab) {\n                throw new Error('No active tab');\n            }\n            \n            // Try to get selected text\n            const [result] = await chrome.scripting.executeScript({\n                target: { tabId: tab.id },\n                function: () => window.getSelection().toString().trim()\n            });\n            \n            const selectedText = result.result;\n            \n            if (selectedText && selectedText.length > 10) {\n                await this.captureSelection(selectedText, tab);\n            } else {\n                // Fallback to page capture\n                await this.capturePage(tab);\n            }\n            \n        } catch (error) {\n            console.error('Quick capture failed:', error);\n            this.showNotification('❌ Quick Capture Failed', error.message);\n        }\n    }\n    \n    /**\n     * Toggle capture mode\n     */\n    async toggleCaptureMode() {\n        const newMode = !this.settings.autoDetect;\n        await this.updateSetting('autoDetect', newMode);\n        \n        this.showNotification(\n            newMode ? '🧠 Auto-Capture Enabled' : '🧠 Auto-Capture Disabled',\n            newMode ? 'AI responses will be automatically detected' : 'Manual capture only'\n        );\n    }\n    \n    /**\n     * Perform periodic health check\n     */\n    async performHealthCheck() {\n        try {\n            const response = await fetch(`${this.apiBase}/api/health`, {\n                method: 'GET',\n                timeout: 5000\n            });\n            \n            if (!response.ok) {\n                console.warn('⚠️ Memory service health check failed');\n            }\n            \n        } catch (error) {\n            console.warn('⚠️ Memory service unreachable during health check:', error.message);\n        }\n    }\n    \n    /**\n     * Show welcome notification on first install\n     */\n    async showWelcomeNotification() {\n        this.showNotification(\n            '🧠 Universal AI Memory Capture',\n            'Extension installed! Visit AI platforms to start capturing insights.',\n            'welcome'\n        );\n    }\n    \n    /**\n     * Show notification to user\n     */\n    showNotification(title, message, notificationId = null) {\n        if (!this.settings.showNotifications) {\n            return;\n        }\n        \n        const options = {\n            type: 'basic',\n            iconUrl: 'icons/icon48.png',\n            title: title,\n            message: message\n        };\n        \n        const id = notificationId || `notification-${Date.now()}`;\n        \n        chrome.notifications.create(id, options, (notificationId) => {\n            if (chrome.runtime.lastError) {\n                console.error('Notification error:', chrome.runtime.lastError);\n            } else {\n                console.log('📱 Notification shown:', notificationId);\n                \n                // Auto-clear after 5 seconds\n                setTimeout(() => {\n                    chrome.notifications.clear(notificationId);\n                }, 5000);\n            }\n        });\n    }\n}\n\n// Initialize service worker\nconst memoryServiceWorker = new MemoryServiceWorker();